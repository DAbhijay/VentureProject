apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: ai-task-platform
data:
  NODE_ENV: production
  REDIS_HOST: redis
  REDIS_PORT: "6379"
  REDIS_QUEUE_KEY: task_queue
  FRONTEND_URL: https://aitask.example.com
  VITE_API_BASE_URL: https://aitask.example.com/api
