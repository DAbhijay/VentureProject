name: frontend-ci

on:
  push:
    paths:
      - 'frontend/**'
      - '.github/workflows/frontend-ci.yml'

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm ci
        working-directory: frontend
      - run: npm run lint
        working-directory: frontend
      - run: npm run build
        working-directory: frontend
      - uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - uses: docker/build-push-action@v6
        with:
          context: ./frontend
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/ai-task-frontend:${{ github.sha }}
      - name: Update infra image tag
        env:
          GH_TOKEN: ${{ secrets.INFRA_REPO_TOKEN }}
        run: |
          git clone https://x-access-token:${GH_TOKEN}@github.com/your-org/ai-task-platform-infra.git infra-repo
          sed -i "s|ai-task-frontend:.*|ai-task-frontend:${GITHUB_SHA}|" infra-repo/k8s/frontend.yaml
          cd infra-repo
          git config user.email "github-actions@github.com"
          git config user.name "github-actions"
          git add k8s/frontend.yaml
          git commit --trailer "Co-authored-by: Cursor <cursoragent@cursor.com>" -m "chore: update frontend image ${GITHUB_SHA}" || true
          git push
